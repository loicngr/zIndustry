export enum EKey {
    Right = 'KeyD',
    Left = 'KeyA',
    Up = 'KeyW',
    Down = 'KeyS',
    E = 'KeyE'
}

export enum EComponentType {
    Player,
    Fence,
    Door
}

export enum EDirection {
    Up = 'up',
    Down = 'down',
    Right = 'right',
    Left = 'left',
    None = 'down'
}